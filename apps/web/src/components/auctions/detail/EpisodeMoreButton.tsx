'use client';

import React from 'react';

const EpisodeMoreButton = () => {
  return (
    <button
      className="cursor-pointer text-xs text-[#5B80C2]"
      onClick={() => {
        // setSelectedStory(story);
        // setShowStoryModal(true);
      }}
    >
      더보기
    </button>
  );
};

export default EpisodeMoreButton;
